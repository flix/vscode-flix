/// A doc comment
/// that spans
/// three lines.
def main(): Unit \ IO =
    // A line comment
    // that spans
    // multiple lines.
    println(f())

/*
 * A block comment
 * that spans multiple lines.
 */
def f(): Int32 = 42
