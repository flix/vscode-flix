mod JavaMath {
    import java.lang.Math;

    pub def floor(x: Float64): Float64 \ IO =
        let r = Math.f
        r
    
    pub def max(x: Float64): Float64 \ IO =
        let r = Math.m
        r
}
