// Blank line
mod Area {
    // Unused enum + type parameter
    enum Shape[t] {
        // Unused enum case
        case Circle(Int32) // circle radius
    }

    // Unused function + formal parameter
    def area(s: Shape[t]): Int32 = 42
}
